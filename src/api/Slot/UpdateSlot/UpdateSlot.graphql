type UpdateSlotResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  UpdateSlot(
    slotId: Int!
    isFulltime: Boolean
    startTime: String
    isEndTimeNextDay: Boolean
    isStartTimeNextDay: Boolean
    endTime: String
    userId: Int
    day: Int
  ): UpdateSlotResponse!
}
