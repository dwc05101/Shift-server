type CreateSlotResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  CreateSlot(
    startTime: String!
    endTime: String!
    dayNumber: Int!
    timetableId: Int!
    personalCode: String!
    organizationId: Int!
  ): CreateSlotResponse!
}
