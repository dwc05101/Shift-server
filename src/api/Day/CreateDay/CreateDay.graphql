type CreateDayResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  CreateDay(
    startTime: String!
    endTime: String!
    dayNumber: Int!
    timeTableId: Int!
  ): CreateDayResponse!
}
