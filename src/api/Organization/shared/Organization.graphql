type Organization {
  id: Int!
  name: String!
  loginId: String!
  password: String!
  profilePhoto: String!
  users: [User]
  links: [Link]
  timetables: [TimeTable]
  createdAt: String!
  updatedAt: String
}
