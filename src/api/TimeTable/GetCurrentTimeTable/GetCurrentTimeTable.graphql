type GetCurrentTimeTableResponse {
  ok: Boolean!
  error: String
  timetable: TimeTable
}

type Query {
  GetCurrentTimeTable(
    yearMonthWeek: String
    timetableId: Int
    organizationId: Int
  ): GetCurrentTimeTableResponse!
}
